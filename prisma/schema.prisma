generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model runs {
  date     String @unique
  distance String
  duration String
  id       Int    @id @default(autoincrement())
}
